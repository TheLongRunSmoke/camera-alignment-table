/**
 * Simple class for rectangle representation.
 * Nothing special, just are few public fields. 
 */
export default class Rect {
    constructor(topLeftX, topLeftY, width, height) {
        this.topLeftX = topLeftX
        this.topLeftY = topLeftY
        this.width = width
        this.height = height
    }
}